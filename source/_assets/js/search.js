document.addEventListener('DOMContentLoaded', function () {
    // Overlay, fetch, and Fuse land in later steps. This file exists so Vite
    // emits a hashed module on every page without changing behaviour yet.
});
